const crypto=require('crypto');module.exports=async(req,res)=>{res.setHeader('Access-Control-Allow-Origin','https://kothabolbo.vercel.app');if(req.method==='OPTIONS')return res.status(204).end();const key=process.env.IMAGEKIT_PRIVATE_KEY||process.env.IK_PRIVATE_KEY;if(!key)return res.status(500).json({error:'Missing IMAGEKIT_PRIVATE_KEY'});const token=crypto.randomBytes(16).toString('hex'),expire=Math.floor(Date.now()/1000)+1800;res.json({token,expire,signature:crypto.createHmac('sha1',key).update(token+expire).digest('hex')});};
